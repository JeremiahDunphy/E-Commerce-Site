// This is a simple, valid next.config.js file.

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Any additional configuration options go here
};

module.exports = nextConfig;
